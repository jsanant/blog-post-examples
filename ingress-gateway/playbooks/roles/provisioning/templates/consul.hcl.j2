datacenter = "dc1"

data_dir   = "/var/lib/consul"

log_level  = "INFO"

server    = true
bootstrap = true

ui_config {
  enabled = true
}

client_addr = "{{ ansible_eth1.ipv4.address }}"
bind_addr = "{{ ansible_eth1.ipv4.address }}"